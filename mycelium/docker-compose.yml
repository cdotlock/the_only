services:
  relay:
    build: .
    ports:
      - "8470:8470"
    volumes:
      - relay_data:/app/data
    environment:
      - RELAY_DB_PATH=/app/data/mycelium.db
      - RELAY_NAME=Mycelium Relay
      - MIN_QUALITY=6.0
      - LOG_LEVEL=INFO
    restart: unless-stopped

volumes:
  relay_data:
